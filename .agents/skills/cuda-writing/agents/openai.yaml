interface:
  display_name: "Cuda Writing"
  short_description: "Design GPU code from workload shape to kernel launch."
  default_prompt: "Use $cuda-writing to declare the ADR-0046 physical workload shape, choose ADR-0044 native carriers, then select CUDA/CuPy/CCCL primitives and launch/runtime patterns."
