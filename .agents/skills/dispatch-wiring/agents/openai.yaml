interface:
  display_name: "Dispatch Wiring"
  short_description: "Wire public APIs to native carriers and fallback boundaries."
  default_prompt: "Use $dispatch-wiring to map a public API or owned helper through dispatch while preserving ADR-0044 native carriers, ADR-0046 shape estimates, and observable fallback/export boundaries."
