interface:
  display_name: "Cuda Optimizer"
  short_description: "Optimize GPU code, starting with physical workload shape."
  default_prompt: "Use $cuda-optimizer to review an existing GPU module for ADR-0046 physical-shape failures, native-carrier reuse, host/device boundaries, and concrete performance rewrites."
