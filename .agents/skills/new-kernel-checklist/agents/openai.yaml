interface:
  display_name: "New Kernel Checklist"
  short_description: "Checklist for new shape-first GPU operations."
  default_prompt: "Use $new-kernel-checklist to declare the physical workload shape and verify every required carrier, precision, dispatch, warmup, test, benchmark, and docs step for a new GPU operation."
