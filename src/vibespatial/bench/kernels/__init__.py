"""NVBench kernel benchmark scripts (Tier 2).

Each ``bench_*.py`` script in this directory is a standalone NVBench program
that is auto-discovered and spawned as a subprocess by ``nvbench_runner.py``.
"""
